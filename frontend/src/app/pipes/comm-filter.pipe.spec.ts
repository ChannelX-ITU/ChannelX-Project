import { CommFilterPipe } from './comm-filter.pipe';

describe('CommFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
